<div class="row">
  <div class="ten columns">
    <h2>Editing <%= @person.name %>'s details</h2>
    <%= simple_form_for @person do |f| %>
      <%= f.input :name %>
      <%= f.input :email %>
      <%= f.button :submit, "Save" %>
    <% end %>
  </div>
</div>
