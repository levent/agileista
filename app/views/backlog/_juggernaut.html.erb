var update = function(data){
  notifyUser(data, "<%= current_user.name %>");
};

var source = new EventSource('/subscribe/<%= uid %>');

source.addEventListener('message', function (event) {
  update(JSON.parse(event.data));
}, false);
