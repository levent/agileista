<div id="navii">
    <ul>
        <li><%= link_to "Back to sprint planning", :controller => 'sprint_planning', :action => 'show', :id => params[:sprint_id] unless params[:from] && params[:from] == 'tb' %></li>
        <li><%= link_to "Back to task board", :controller => 'sprint', :action => 'index' if params[:from] && params[:from] == 'tb' %></li>
<% if @current_user.is_a?(TeamMember) -%>
        <li><%= link_to "Delete task!!", :controller => 'user_stories', :action => 'destroy_task', :id => @user_story, :task_id => params[:task_id] %></li>
<% end -%>
    </ul>
    <div class="c">&nbsp;</div>
</div>
<div id="main">
	<h1>Show task:</h1>
	<div id="leftcontent">
		<h2>User story details:</h2>
		<p><span class="detail">Title: </span><%=h @user_story.definition %><br />
		<span class="detail">Notes: </span><%=h @user_story.description %><br />
		<span class="detail">Story points: </span><%= show_story_points(@user_story.story_points) %><br />
		<span class="detail">Hours left: </span><%= @user_story.tasks.sum('hours') %><br />
		<span class="detail">Tasks: </span><%= @user_story.tasks.count %></p>
		<h3>Acceptance Criteria:</h3>
		<ul class="nobullet">
		<% for crit in @user_story.acceptance_criteria %>
			<li><%=h crit.detail %></li>
		<% end %>
		</ul>
		<h3>All Tasks:</h3>
		<ul class="nobullet">
		<% for task in @user_story.tasks -%>
			<li><%= link_to 'View', :action => 'show_task', :id => @user_story, :task_id => task, :from => 'tasks', :sprint_id => @user_story.sprint.id %> <%= link_to 'Edit', :action => 'edit_task', :id => @user_story, :task_id => task, :from => 'tasks', :sprint_id => @user_story.sprint.id %> <%=h task.definition %></li>
		<% end -%>
		</ul>
	</div>
	<div id="rightcontent">
		<h2><%=h @task.definition %></h2>
		<h3>Description:</h3>
		<p><%= h@task.description %></p>
		<h3>Hours left:</h3>
		<p><%= @task.hours %>
		<h3>Assigned to:</h3>
		<p><%= show_assignee(@task.developer) %></p>
	</div>
</div>